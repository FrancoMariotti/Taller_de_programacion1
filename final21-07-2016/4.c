/*	Escriba:
		. La declaracion de una funcion de alcance local al fuente donde se declaro que tome como parametro un puntero a entero con signo y devuelva un puntero a puntero a caracter sin signo.
		. La definicion de un puntero a caracter.
		. La declaracion de una funcion qe tome como parametros dos enteros y devuelva un entero.
*/ 

static unsigned char** foo(signed int* a);

char* ptr = 255; 

int sum(int a,int b);